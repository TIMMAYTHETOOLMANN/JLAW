apiVersion: v1
kind: Service
metadata:
  name: jlaw-forensics
  namespace: jlaw-forensics
  labels:
    app: jlaw
    component: forensics-engine
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: jlaw
    component: forensics-engine
